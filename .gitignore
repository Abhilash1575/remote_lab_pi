-e vlab.db
__pycache__/
*.pyc
.env
battery_status.json
ups_log.csv

# Per-Pi experiment content: each Lab Pi hosts a different board/experiment,
# so its UI config, admin password hash, uploaded/factory-reset firmware,
# and manual PDF are local state, not shared source.
data/
