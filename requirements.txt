flask>=2.0.0
flask-socketio>=5.0.0
flask-login>=0.6.0
flask-mail>=0.9.0
flask-sqlalchemy>=3.0.0
werkzeug>=2.0.0
sqlalchemy>=2.0.0
py-bcrypt>=0.4
pyserial>=3.5
lgpio>=0.2.0.0
eventlet>=0.33.0
python-socketio>=5.0.0
requests>=2.28.0
psutil>=5.9.0
python-dotenv>=0.19.0
numpy>=1.20.0
scipy>=1.7.0
# Hardware debugger (GDB/OpenOCD) -- see debug/. openocd and a GDB binary
# (gdb-multiarch on Raspberry Pi OS/Debian) are OS packages, not pip --
# install separately (apt install openocd gdb-multiarch).
pygdbmi>=0.11
# Audio streaming dependencies
aiohttp>=3.8.0
aiortc>=1.5.0
# aiortc's real WebRTC media path (audio encoding/resampling) hard-requires
# av (PyAV) at import time -- it is not optional. Pin within aiortc's
# declared compatible range; newer av releases have broken API changes.
av>=14.0.0,<17.0.0
